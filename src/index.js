
import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css'; // Make sure this is pointing to your CSS file

import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
